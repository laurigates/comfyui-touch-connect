{
  "name": "comfyui-touch-connect",
  "private": true,
  "type": "module",
  "description": "Magnifier loupe under your finger for accurate node-connection dragging on touch devices. TypeScript source in src/, built to web/dist/ via bun build. See ADR-0001 (TS migration).",
  "scripts": {
    "build": "bun build ./src/index.ts --target browser --format esm --outdir web/dist --external '/scripts/*'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome check",
    "knip": "knip"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "@comfyorg/comfyui-frontend-types": "^1.43.0",
    "@biomejs/biome": "^2.4.15",
    "vitest": "^4.1.7",
    "knip": "^5.0.0"
  }
}
